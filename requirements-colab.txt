# Dependencias para o fine-tuning no Google Colab (GPU T4, Python 3.11).
# O Colab ja traz torch com CUDA pre-instalado - NAO reinstale torch aqui.
#
#   !pip install -q -r requirements-colab.txt

transformers==4.57.0
trl==0.24.0
peft==0.17.1
accelerate==1.10.1
bitsandbytes==0.48.1
datasets==4.0.0
