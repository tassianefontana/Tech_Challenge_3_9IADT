# ============================================================
# Tech Challenge Fase 3 - Assistente Medico
# ============================================================
# Ambiente LOCAL recomendado: Python 3.11 (nao use 3.13+, pois
# torch/bitsandbytes ainda nao possuem wheels estaveis).
# O fine-tuning (Etapa 3) roda no Google Colab, ver notebooks/.
# ============================================================

# --- Preprocessing / dados ---
# (Etapa 2 usa apenas a biblioteca padrao do Python)

# --- RAG / LangChain (Etapas 4 e 5) ---
langchain==0.3.27
langchain-core==0.3.78
langchain-community==0.3.30
langchain-huggingface==0.3.1
langchain-chroma==0.2.6
langgraph==0.6.8
chromadb==1.1.1
sentence-transformers==5.1.1

# --- Modelo / inferencia local ---
transformers==4.57.0
torch==2.8.0
accelerate==1.10.1
peft==0.17.1

# --- Base estruturada de pacientes (Etapa 4) ---
# sqlite3 faz parte da stdlib

# --- Avaliacao (Etapa 3) ---
evaluate==0.4.6
rouge-score==0.1.2
scikit-learn==1.7.2

# --- Utilitarios ---
python-dotenv==1.1.1
tqdm==4.67.1
